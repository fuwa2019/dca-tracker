# Objective Evidence Audit

Audited: 2026-08-18

This is an evidence ledger for the full competitive-research and six-month
objective. `proved` means current repository/runtime evidence covers the stated
scope. `partial` means a meaningful local slice exists but the full requirement
is not proven. `missing` means no current evidence is sufficient.

| Requirement | Status | Current evidence | Missing proof |
|---|---|---|---|
| Product remains one private single ETF portfolio | proved | `PRODUCT.md`, `DESIGN.md`, scope tests and no account-expansion code | none for this boundary |
| Fixed Ghostfolio/Wealthfolio/Portfolio Performance versions recorded | proved | `sources.md`, release links, drift notes | none for source snapshot |
| Fixed-version first launch/import/re-import/ledger/performance/share/mobile study | partial | Ghostfolio S1 through T10 partial plus authenticated Demo home/portfolio/activities/account routes at 390x844, no-overflow measurements, tablist keyboard route cycle and reduced-motion stylesheet evidence; Wealthfolio S1 T01-T04,T06-T08 plus T05/T09 capability checks, narrow native Activities/Appearance layouts, keyboard paths and source breakpoint scan; Portfolio Performance T01-T09 desktop plus a 392x700 native narrow capture showing clipped columns; fixed DMGs and local screenshots | Ghostfolio header/menu full keyboard traversal, reduced-motion emulation and non-empty authenticated ledger state; Wealthfolio runtime reduced-motion and full cross-route keyboard audit; Portfolio Performance responsive remediation/keyboard/reduced-motion evidence and public-share comparison; absent Wealthfolio source-replacement/public-share capabilities are recorded but cannot satisfy positive workflow coverage |
| 100-point scorecard and tie-break rule | proved | `scorecard.md` | observed product scores |
| Synthetic ledger covers required events and errors | proved | canonical ledger, ordinary-close prices, Schwab/IBKR/TradingView fixtures, fixed-point verifier | none for fixture shape |
| `PortfolioImportAdapter` contract | proved | `src/lib/import/types.ts`, `common.ts` | persistence integration |
| Schwab/IBKR/TradingView parsing and normalization | partial | adapter regression, official IBKR English/Chinese-shaped fixtures, TradingView round-trip | full provider exports and ETF classification policy against fixed apps |
| Unified preview with four row states and reconciliation | partial | `PortfolioImportTools`, Playwright desktop/390px checks, fixed-point reconciliation | cloud-enabled RPC run and production migration |
| `append` idempotency | proved locally | local PostgreSQL 15.19 RPC run: repeat append added zero and returned unchanged rows | Supabase `main` remains on legacy schema until separately authorized |
| `replace_source` isolation | proved locally | local PostgreSQL 15.19 run preserved manual and IBKR rows while replacing TradingView rows | Supabase `main` remains on legacy schema until separately authorized |
| `reset_all` atomic rollback | proved locally | forced oversell in local PostgreSQL rolled back transactions and cashflows | Supabase `main` remains on legacy schema until separately authorized |
| `cashflows` V2 fields and event kinds | proved locally | all 52 repository migrations applied through `0050`; columns, constraints and triggers inspected | `main` schema equivalence after an authorized migration |
| Auth/RLS/advisory-lock RPC | proved locally, scope-limited | two local `authenticated` sessions, owner RLS isolation, advisory lock contract and generic RPC invocation passed | Cloud JWT/RLS behavior on `main` after an authorized release |
| V2 cash/NAV/XIRR/TWR formulas | proved against the reference application | pure `ledgerTwr`, cash balance, ledger XIRR, independent formula reconciliation, local cache invalidation, and — added 2026-08-23 — an application gate in `test:finance` that feeds PP 0.86.0's own stored ledger (decoded from the `PPPBV1` protobuf, money in cents and shares in 1e-8 units, pinned in `fixtures/portfolio-performance-stored-ledger.json`) to the shipped engine and reproduces all three displayed figures: value `$921.53`, TTWROR `2.15%`, IRR `3.83%`. The residual `0.3994pp` against our own run is entirely the price input, since PP had no quote provider and valued at the latest transaction's gross price | Reconciliation used PP's no-quote-history valuation because that is the state the application was in; a run with a configured quote history would tighten TTWROR from display precision to sub-basis-point. Maximum drawdown and volatility are unreconciled |
| V1 remains default and V2 is gated | proved | `VITE_LEDGER_IMPORT_V2=0`, method documentation, old cloud path retained | release migration after authorization |
| Dashboard/share use the same V2 cache without amount leakage | partial | 2026-08-23: a static audit of the anonymous surface became `test:share-privacy` in CI. It pins the anon-executable set to the three documented entry points, allowlists every JSON key those entry points emit, forbids id-like and amount-like keys, and requires the cached payload to be projected through `_public_share_sanitize_history`; negative-tested against three mutated migration sets. The audit found a live V1 leak — the cached `warnings` array carries `nav_user`, `nav_benchmark` and `flow` whenever a day is skipped for non-positive NAV, and the share returned the cache wholesale — reproduced on PostgreSQL 15.19 and fixed by migration `0051`. Ghostfolio share snapshot recorded separately; Wealthfolio and Portfolio Performance expose no public-share flow | Migration `0051` is applied to Supabase `main` as version `20260823155818`; live function inspection and a synthetic projection check confirmed the public boundary removes the absolute warning fields. D1 proper still needs the V2 cache/RPC itself, so this row remains partial. The repository gate reads migration text; live drift was checked separately |
| WCAG 2.2 AA for all named routes | partial | Product side (2026-08-20, `docs/accessibility/2026-08-20-wcag-route-audit.md`): axe-core 4.10.2 over nine routes x desktop/390px x light/dark returns zero violations after the fixes in that record; forward/reverse Tab traversal on every locally renderable route hits only interactive elements with a visible focus ring; reduced-motion emulation leaves opacity-only change on all routes; 320px reflow and the 24x24 target-size minimum both hold. Extended 2026-08-23 (sections 7 and 8 of the same record): the settings surface became seven routes and re-scanned at zero violations over 28 scans; 2.4.11 focus-not-obscured was measured for the first time over 691 focus stops on 14 routes at three widths, found failing at 390px behind the fixed bottom nav on 8 stops, fixed with a scroll-margin rule, and re-measured at zero failures and zero partially obscured stops. Extended 2026-08-24 (section 9 of the same record): the accessibility tree a screen reader actually consumes was audited for the first time over 13 routes x desktop/390px = 26 scans, pulled from Chrome 151 over the DevTools Protocol. It found **503 findings the 36 clean axe scans could not see**: every `lucide-react` icon rendered as an unnamed `role=image` (71 of them on `/transactions/all`; axe's `svg-img-alt` rule only fires on `svg[role="img"]`, and these carried no role), and no data table had an accessible name. Fixed by a decorative-by-default icon module, `sr-only` captions on the five tables, and hiding the Recharts drawing inside its already-labelled wrapper; re-measured at 0 findings over 26 scans with axe, keyboard, reduced-motion, reflow and target-size evidence all re-run without regression. Competitor side: Ghostfolio authenticated Demo routes have no 390px overflow and its tablist route cycle is keyboard-operable; Wealthfolio narrow Activities/Appearance layouts expose forward/reverse Tab traversal, visible focus state and keyboard access to visible controls; Portfolio Performance narrow capture exposes clipped columns and no distinct focus state | The mechanical half of the screen-reader pass is now proved (section 9); what remains is a real assistive technology driven for real — VoiceOver/NVDA/JAWS announcement order, live-region timing, braille output, rotor and gesture navigation — which needs system settings changes outside this repository's tooling. Also still open: the cloud-only states (`/cashflows`, a populated `/share/:token`, the authenticated login flow); Ghostfolio header/menu keyboard and reduced-motion emulation; native reduced-motion runtime evidence and Portfolio Performance keyboard/responsive remediation |
| Performance/Lighthouse/compatibility gates | partial | 2026-08-24, recorded in `docs/release/2026-08-24-release-gates.md`. **Budgets: proved.** `npm run test:release-budget` is in CI after the build step and gates first-load gzip weight per category, the largest chunk, request count and the number of render-blocking third-party origins against `docs/release/performance-budget.json`; negative-tested against four mutations (tightened budget, an injected second third-party stylesheet, a 400 KB fattened chunk, a missing `dist/`). **Lighthouse: proved.** `docs/release/probes/lighthouse-budget.mjs` runs Lighthouse 12.8.2 over six routes at both form factors and gates performance, accessibility, best practices, CLS and LCP. Establishing it removed the two render blockers in the document head (the Google Fonts stylesheet, 2,907 ms; the synchronous service-worker registration), halving FCP on both form factors. It also produced the honest finding that the composite performance score moves up to 20 points run to run on this machine, so that score is gated only at a catastrophic floor while the deterministic budget above is the precise ratchet. **Compatibility: partial.** `docs/release/probes/cross-browser-check.mjs` runs one shared payload on real engines with no downloaded browser; Blink passes 18/18 runs (boot, 0 overflow at 1280/390/320, 0 console errors, CSS/JS feature support, the local-date contract, token resolution). | WebKit and Gecko are not covered. `safaridriver` is present and ready but session creation needs Safari's "Allow remote automation" setting turned on by hand; Firefox is not installed and the constraint for this record was to download no browsers. The probe reports both as uncovered rather than passing, and re-running it after the Safari setting is enabled fills the WebKit row with no code change. The authenticated cloud routes, `/cashflows` and a populated `/share/<token>` are outside every probe, which measure the offline demo build. |
| No production DB/deploy/real brokerage file changes | superseded by authorized follow-up | The study window used a read-only Supabase preflight, no auth import, and the feature flag stayed off | On 2026-08-24 the owner-authorized 0051 apply changed only public RPC definitions; no private portfolio rows, brokerage files, or Worker/Pages deployment were touched |

## Study Decision

The competitive-research objective is closed for this study window under the
explicit no-macOS-change boundary. The evidence table preserves partial or
missing statuses where the products or the environment did not provide proof;
those are research findings, not implied passes. Migrations `0050` and `0051`, production
database changes, performance-method switching and deployment remain separate
authorized release gates.
