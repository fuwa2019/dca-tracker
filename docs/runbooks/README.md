# Runbooks

- `development.md`: dependency install, local modes, verification, and smoke
  checks.
- `database-migrations.md`: append-only migration and cache verification flow.
- `deployment.md`: authorized deployment boundaries and component steps.

Runbooks describe safe procedures. They do not authorize production changes.
